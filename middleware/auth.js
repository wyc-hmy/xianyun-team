// 这是测试文件，和业务无关
export default function (context) {

    // context.$axios({
    //     url: "是否登录的接口"
    // }).then(res => {
    //     if("没有登录"){
    //         context.$router.push("user/login")
    //     }
    // })

    // 跳转
    //context.redirect("/user/login")
}